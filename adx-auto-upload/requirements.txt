# ADX-Auto 合法合规依赖清单
# 核心框架
Flask>=2.3,<4.0
Flask-SQLAlchemy>=3.0,<4.0
SQLAlchemy>=1.4,<3.0

# Google Ads API（可选，未安装时自动降级为 Mock 模式）
google-ads>=25.0.0

# AI 创意生成（可选，未安装时自动降级为 Mock 模式）
openai>=1.0.0

# 网络请求
requests>=2.28,<3.0

# 定时任务（可选，未安装时使用简易模式）
croniter>=1.3,<3.0

# 工具
python-dotenv>=1.0,<2.0
Werkzeug>=2.3,<4.0

# 生产环境 WSGI 服务器
gunicorn>=21.0
